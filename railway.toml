[build]
builder = "DOCKER"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uv run python -m src.assignment.main --serve --host 0.0.0.0 --port $PORT"
healthcheckPath = "/docs"
healthcheckTimeout = 120
restartPolicy = "ON_FAILURE"
